import { Box, styled } from "@mui/material";

const CoverPage = styled(Box)({
    display: "flex",
    flexFlow: "column",
    height: "100vh",
    position: "inherit",
    width: "100vw"
});

export default CoverPage;