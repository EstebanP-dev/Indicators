import { Box, styled } from "@mui/material";

const CoverPage = styled(Box)({
    display: "flex",
    flexFlow: "column",
    height: "100vh",
    position: "absolute",
    width: "100vw",
    top: 0,
    left: 0,
    alignItems: "center",
    justifyContent: "center",
});

export default CoverPage;